import SwiftUI
struct Theme {
    let background: Color
    let secondaryBackground: Color
    let primaryText: Color
    let secondaryText: Color
    // Should be an asset that is symbolic for the theme, eg Fire for red, Water for blue etc
    let themeImageAssetName: String
}
